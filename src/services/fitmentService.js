/**
 * Hero vehicle fitment — delegates to API + static fallback (`api/vehicle.js`).
 */
export {
  fetchVehicleBrands,
  fetchVehicleModels,
  fetchVehicleYears,
  fetchVehicleVariants,
  fetchCars,
  submitVehicleSearch,
} from '../api/vehicle.js'

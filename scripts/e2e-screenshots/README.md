# E2E delivery OTP visual captures

Generated during automated E2E run. Open the HTML files in a browser for full-page visual checks.

- `ord_573a64b0-before-verify.html` — customer `/orders` expanded, OTP visible
- `ord_573a64b0-after-verify.html` — same view after partner verified OTP

API transcript: `e2e-delivery-otp-report.json` (if script completed).

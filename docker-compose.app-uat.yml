services:
  api:
    image: ${CARNALYSYS_API_IMAGE}
    env_file:
      - ./backend/.env.uat
    environment:
      SPRING_PROFILES_ACTIVE: uat
    ports:
      - "${SERVER_PORT:-8080}:8080"
    restart: unless-stopped

